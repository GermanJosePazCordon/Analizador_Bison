#ifndef OBMKDIR_H
#define OBMKDIR_H
#include <QString>
#include "string"
using namespace std;

class obmkdir
{
public:
    obmkdir();
    int space;
    int size;

    void mostrar(obmkdir *disc);
};

#endif // OBMKDIR_H
